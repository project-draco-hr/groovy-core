{
  BufferedWriter writer=null;
  try {
    writer=newWriter(self,charset);
    writer.write(text);
    writer.flush();
    Writer temp=writer;
    writer=null;
    temp.close();
  }
  finally {
    closeWithWarning(writer);
  }
}
