{
  if (value.matches(".*&.*")) {
    value=value.replaceAll("&","&amp;");
  }
  if (value.matches(".*<.*")) {
    value=value.replaceAll("<","&lt;");
  }
  if (value.matches(".*>.*")) {
    value=value.replaceAll(">","&gt;");
  }
  if (isAttrValue) {
    if (value.matches(".*\\'.*")) {
      value=value.replaceAll("\\'","&apos;");
    }
  }
  return value;
}
