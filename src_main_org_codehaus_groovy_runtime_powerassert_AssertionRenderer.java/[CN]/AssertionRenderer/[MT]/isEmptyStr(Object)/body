{
  Class<?> clazz=value.getClass();
  if ((clazz == String.class || clazz == StringBuffer.class || clazz == StringBuilder.class) && value.toString().equals("")) {
    return true;
  }
  return false;
}
