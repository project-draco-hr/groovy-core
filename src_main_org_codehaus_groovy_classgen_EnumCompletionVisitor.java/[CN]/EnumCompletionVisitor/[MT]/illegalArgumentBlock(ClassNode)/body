{
  BlockStatement outerElseBlock=new BlockStatement();
  outerElseBlock.addStatement(new ThrowStatement(new ConstructorCallExpression(ClassHelper.make(IllegalArgumentException.class),new ArgumentListExpression(new ConstantExpression("One of the enum constants for enum " + enumClass.getName() + " was initialized with null. Please use a non-null value or define your own constructor.")))));
  return outerElseBlock;
}
