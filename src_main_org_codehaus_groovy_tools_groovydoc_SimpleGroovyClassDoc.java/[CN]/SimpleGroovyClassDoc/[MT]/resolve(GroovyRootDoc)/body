{
  if (superClassName != null) {
    superClass=rootDoc.classNamed(superClassName);
    if (superClass == null) {
      superClass=new SimpleGroovyClassDoc(superClassName);
    }
  }
 else {
    superClass=new SimpleGroovyClassDoc("Object");
  }
}
