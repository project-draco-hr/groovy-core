{
  super("No signature of method: " + (isStatic ? "static " : "") + type.getName()+ "."+ method+ "() is applicable for argument types: ("+ InvokerHelper.toTypeString(arguments)+ ") values: "+ InvokerHelper.toString(arguments));
  this.method=method;
  this.type=type;
  this.isStatic=isStatic;
}
