{
  BlockStatement block=new BlockStatement();
  block.addStatement(new ThrowStatement(new ConstructorCallExpression(ClassHelper.make(IllegalArgumentException.class),new ArgumentListExpression(new ConstantExpression(message)))));
  return block;
}
