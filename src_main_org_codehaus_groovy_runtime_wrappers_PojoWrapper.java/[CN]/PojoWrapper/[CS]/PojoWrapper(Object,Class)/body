{
  super(constrainedType);
  this.wrapped=wrapped;
  if (wrapped instanceof GroovyObject) {
    throw new RuntimeException("trying to wrap the groovyObject " + wrapped.getClass().getName() + " in a PojoWrapper");
  }
}
