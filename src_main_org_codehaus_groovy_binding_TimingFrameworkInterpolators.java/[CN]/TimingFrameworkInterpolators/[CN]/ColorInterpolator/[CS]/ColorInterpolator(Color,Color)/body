{
  super(null);
  setResolveStrategy(TO_SELF);
  rb=c1.getRed();
  rr=c2.getRed() - c1.getRed();
  gb=c1.getGreen();
  gr=c2.getGreen() - c1.getGreen();
  bb=c1.getBlue();
  br=c2.getBlue() - c1.getBlue();
  ab=c1.getAlpha();
  ar=c2.getAlpha() - c1.getAlpha();
}
