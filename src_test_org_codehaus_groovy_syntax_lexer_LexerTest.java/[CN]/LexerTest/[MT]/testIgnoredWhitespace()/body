{
  newLexer(" \r \n \r\n \n\r    \t   \t");
  assertNextToken(Token.NEWLINE,"<newline>");
  assertNextToken(Token.NEWLINE,"<newline>");
  assertNextToken(Token.NEWLINE,"<newline>");
  assertEnd();
}
