{
  CSTNode expr=unaryExpression();
  LOOP:   while (true) {
    SWITCH: switch (lt()) {
case (Token.MULTIPLY):
case (Token.DIVIDE):
case (Token.MOD):
case (Token.COMPARE_TO):
{
        expr=rootNode(lt(),expr);
        expr.addChild(unaryExpression());
        break SWITCH;
      }
default :
{
      break LOOP;
    }
}
}
return expr;
}
