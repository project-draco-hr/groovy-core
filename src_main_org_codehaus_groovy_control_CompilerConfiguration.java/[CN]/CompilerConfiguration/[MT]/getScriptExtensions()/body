{
  if (scriptExtensions == null || scriptExtensions.isEmpty()) {
    scriptExtensions=SourceExtensionHandler.getRegisteredExtensions(this.getClass().getClassLoader());
  }
  return scriptExtensions;
}
