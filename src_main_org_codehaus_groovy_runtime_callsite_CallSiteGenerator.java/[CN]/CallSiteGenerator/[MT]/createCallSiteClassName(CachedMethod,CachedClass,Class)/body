{
  final String name;
  if (declClass.getName().startsWith("java."))   name=acls.getName().replace('.','_') + "$" + cachedMethod.getName();
 else   name=declClass.getName() + "$" + cachedMethod.getName();
  return name;
}
