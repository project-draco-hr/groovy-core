{
  int n=Integer.parseInt(args[0]);
  int i, c;
  String s="";
  Integer ii;
  HashMap ht=new HashMap();
  c=0;
  for (i=1; i <= n; i++)   ht.put(Integer.toString(i,16),new Integer(i));
  for (i=1; i <= n; i++)   if (ht.containsKey(Integer.toString(i,10)))   c++;
  System.out.println(c);
}
