{
  if (SwingBuilder.checkValueIsTypeNotString(value,name,JLabel.class)) {
    return value;
  }
  if (value instanceof String) {
    return new JLabel((String)value);
  }
 else {
    return new JLabel();
  }
}
