{
  if (value == null) {
    return new NodeList();
  }
 else   if (value instanceof List) {
    return (List)value;
  }
 else {
    NodeList result=new NodeList();
    result.add(value);
    return result;
  }
}
