{
  ArrayExpression conversion=new ArrayExpression(expr.getElementType(),rewriteAll(expr.getExpressions()),rewriteAll(expr.getSizeExpression()));
  conversion.setSourcePosition(expr);
  result=conversion;
}
