{
  CastExpression conversion=new CastExpression(expr.getType(),rewrite(expr.getExpression()),expr.isIgnoringAutoboxing());
  conversion.setSourcePosition(expr);
  conversion.setCoerce(expr.isCoerce());
  result=conversion;
}
