{
  if (obj == this) {
    return true;
  }
  if (!(obj instanceof QName)) {
    return false;
  }
  if ((namespaceURI.equals(((QName)obj).namespaceURI)) && (localPart == ((QName)obj).localPart)) {
    return true;
  }
  return false;
}
