{
  ComplexKeyHashMap.Entry[] table=from.getTable();
  int len=table.length;
  for (int i=0; i != len; ++i) {
    for (SingleKeyHashMap.Entry element=(SingleKeyHashMap.Entry)table[i]; element != null; element=(SingleKeyHashMap.Entry)element.next)     copyNonPrivateMethod(element,to);
  }
}
