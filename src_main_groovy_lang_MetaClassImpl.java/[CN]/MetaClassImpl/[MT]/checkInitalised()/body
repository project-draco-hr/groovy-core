{
  if (!initialised)   throw new IllegalStateException("initialise must be called for meta " + "class of " + theClass + "("+ this.getClass()+ ") "+ "to complete initialisation process "+ "before any invocation of field/property "+ "access can be done");
}
