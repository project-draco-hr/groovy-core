{
  if (expression.getOperation().getType() == Types.LEFT_SQUARE_BRACKET && expression.getRightExpression() instanceof MapEntryExpression) {
    addError("You tried to use a map entry for an index operation, this is not " + "allowed. Maybe something should be set in parentheses?",expression.getRightExpression());
  }
  super.visitBinaryExpression(expression);
}
