{
  boolean failed=false;
  for (int i=0; i < expressions.size(); i++) {
    Expression expression=(Expression)expressions.get(i);
    expression.resolve(resolver);
    if (expression.isResolveFailed())     failed=true;
  }
  setResolveFailed(failed);
}
