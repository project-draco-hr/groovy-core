{
  try {
    writer.write("Interceptor ");
    writer.write(origin);
    writer.write(" ");
    writeInfo(object.getClass(),methodName,arguments);
    writer.write("\n");
    writer.flush();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
