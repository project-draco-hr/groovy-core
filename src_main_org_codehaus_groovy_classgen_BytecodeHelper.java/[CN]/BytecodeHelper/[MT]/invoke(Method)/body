{
  int op=Modifier.isStatic(meth.getModifiers()) ? INVOKESTATIC : (meth.getDeclaringClass().isInterface() ? INVOKEINTERFACE : INVOKEVIRTUAL);
  cv.visitMethodInsn(op,getClassInternalName(meth.getDeclaringClass().getName()),meth.getName(),getMethodDescriptor(meth));
}
