{
  if (type != Type.OBJECT_TYPE) {
    if (type.isPrimitiveType() && type != Type.VOID_TYPE) {
      unbox(type);
    }
 else {
      cv.visitTypeInsn(CHECKCAST,type.isArray() ? getTypeDescription(type) : getClassInternalName(type));
    }
  }
}
