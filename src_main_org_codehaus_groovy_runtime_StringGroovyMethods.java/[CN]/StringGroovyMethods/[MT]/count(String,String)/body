{
  int answer=0;
  for (int idx=0; true; idx++) {
    idx=self.indexOf(text,idx);
    if (idx >= 0) {
      ++answer;
      if (idx == self.length())       break;
    }
 else {
      break;
    }
  }
  return answer;
}
