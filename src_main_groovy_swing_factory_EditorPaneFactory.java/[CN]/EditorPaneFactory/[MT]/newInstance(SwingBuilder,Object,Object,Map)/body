{
  if (SwingBuilder.checkValueIsTypeNotString(value,name,JEditorPane.class)) {
    return value;
  }
  if (value instanceof String) {
    properties.put("text",value);
  }
  return new JEditorPane();
}
