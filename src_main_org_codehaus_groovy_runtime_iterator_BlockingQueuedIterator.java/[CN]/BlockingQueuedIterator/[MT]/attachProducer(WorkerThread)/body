{
synchronized (lock) {
    if (!producers.contains(thread)) {
      thread.threadIndex=producers.size();
      producers.add(thread);
      thread.start();
    }
 else     throw new GroovyRuntimeException("Internal Error");
  }
}
