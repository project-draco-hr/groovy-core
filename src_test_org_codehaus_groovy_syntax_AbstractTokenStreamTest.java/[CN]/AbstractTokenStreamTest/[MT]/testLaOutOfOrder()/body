{
  Token[] tokens=new Token[]{Token.newIdentifier("cheeseIt",1,1),Token.newSymbol("(",1,10),Token.newSymbol(")",1,11)};
  MockTokenStream in=new MockTokenStream(tokens);
  assertSame(tokens[2],in.la(3));
  assertSame(tokens[1],in.la(2));
  assertSame(tokens[0],in.la(1));
}
