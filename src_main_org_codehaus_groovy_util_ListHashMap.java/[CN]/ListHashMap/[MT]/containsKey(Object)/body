{
  if (size < maxListFill) {
    for (int i=0; i < listFill; i++) {
      if (listKeys[i].equals(key))       return true;
    }
    return false;
  }
 else {
    return innerMap.containsKey(key);
  }
}
