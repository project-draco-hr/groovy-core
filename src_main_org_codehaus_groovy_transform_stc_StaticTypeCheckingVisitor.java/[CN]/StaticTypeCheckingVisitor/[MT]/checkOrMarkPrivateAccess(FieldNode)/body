{
  if (fn != null && Modifier.isPrivate(fn.getModifiers()) && (fn.getDeclaringClass() != classNode || closureExpression != null) && fn.getDeclaringClass().getModule() == classNode.getModule()) {
    addPrivateFieldOrMethodAccess(fn.getDeclaringClass(),StaticTypesMarker.PV_FIELDS_ACCESS,fn);
  }
}
