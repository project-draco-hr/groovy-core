{
  try {
    return server.getAttribute(name,property);
  }
 catch (  MBeanException e) {
    if (!ignoreErrors)     throw new GroovyRuntimeException("Could not access property: " + property + ". Reason: "+ e,e.getTargetException());
  }
catch (  Exception e) {
    if (!ignoreErrors)     throw new GroovyRuntimeException("Could not access property: " + property + ". Reason: "+ e,e);
  }
  return null;
}
