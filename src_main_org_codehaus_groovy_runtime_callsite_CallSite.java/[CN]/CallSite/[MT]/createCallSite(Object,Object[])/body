{
  if (receiver instanceof Class)   return createCallStaticSite((Class)receiver,args);
  if (!(receiver instanceof GroovyObject)) {
    return createPojoSite(receiver,args);
  }
  return createPogoSite(receiver,args);
}
