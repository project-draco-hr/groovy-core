{
  if (isArray == null) {
    isArray=new boolean[parameterTypes.length];
    for (int i=0; i != parameterTypes.length; ++i)     isArray[i]=parameterTypes[i].isArray();
  }
  return isArray[index];
}
