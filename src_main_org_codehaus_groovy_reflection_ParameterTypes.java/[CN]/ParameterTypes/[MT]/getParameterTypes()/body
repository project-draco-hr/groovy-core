{
  if (parameterTypes == null) {
    if (nativeParamTypes == null)     nativeParamTypes=getPT();
    parameterTypes=new CachedClass[nativeParamTypes.length];
    for (int i=0; i != nativeParamTypes.length; ++i)     parameterTypes[i]=ReflectionCache.getCachedClass(nativeParamTypes[i]);
  }
  return parameterTypes;
}
