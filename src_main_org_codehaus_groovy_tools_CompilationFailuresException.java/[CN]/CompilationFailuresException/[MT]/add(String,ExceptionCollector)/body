{
  if (failures.containsKey(source)) {
    try {
      ((ExceptionCollector)failures.get(source)).merge(exceptions,false);
    }
 catch (    Exception e) {
    }
  }
 else {
    failures.put(source,exceptions);
  }
}
