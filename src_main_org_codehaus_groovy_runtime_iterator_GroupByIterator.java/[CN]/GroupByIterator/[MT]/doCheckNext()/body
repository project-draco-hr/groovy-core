{
  if (!delegate.hasNext()) {
    hasNext=false;
    return;
  }
  ArrayList answer=new ArrayList(bucketSize);
  for (int i=0; i < bucketSize && delegate.hasNext(); ++i)   answer.add(delegate.next());
  lastAcquired=answer;
  hasNext=true;
}
