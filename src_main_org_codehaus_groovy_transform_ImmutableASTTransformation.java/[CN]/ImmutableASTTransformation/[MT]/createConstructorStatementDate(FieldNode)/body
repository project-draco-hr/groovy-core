{
  final FieldExpression fieldExpr=new FieldExpression(fNode);
  Expression initExpr=fNode.getInitialValueExpression();
  if (initExpr == null)   initExpr=ConstantExpression.NULL;
  final Expression date=findArg(fNode.getName());
  return new IfStatement(equalsNullExpr(date),new IfStatement(equalsNullExpr(initExpr),assignField(fieldExpr,ConstantExpression.NULL),assignField(fieldExpr,cloneDateExpr(initExpr))),assignField(fieldExpr,cloneDateExpr(date)));
}
