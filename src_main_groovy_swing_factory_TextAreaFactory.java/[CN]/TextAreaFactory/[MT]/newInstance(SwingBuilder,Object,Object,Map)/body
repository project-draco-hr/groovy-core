{
  if (SwingBuilder.checkValueIsTypeNotString(value,name,JTextArea.class)) {
    return value;
  }
  if (value instanceof String) {
    return new JTextArea((String)value);
  }
 else {
    return new JTextArea();
  }
}
