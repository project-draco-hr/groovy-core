{
  try {
    return receiver.getClass() == metaClass.getTheClass() && checkMetaClass() && MetaClassHelper.sameClasses(params,args);
  }
 catch (  NullPointerException e) {
    if (receiver == null)     return checkAcceptCall(NullObject.getNullObject(),args);
    throw e;
  }
}
