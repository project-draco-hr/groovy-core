{
  try {
    return receiver.getClass() == metaClass.getTheClass() && checkPojoMetaClass() && MetaClassHelper.sameClasses(params,arg);
  }
 catch (  NullPointerException e) {
    if (receiver == null)     return checkCall(NullObject.getNullObject(),arg);
    throw e;
  }
}
