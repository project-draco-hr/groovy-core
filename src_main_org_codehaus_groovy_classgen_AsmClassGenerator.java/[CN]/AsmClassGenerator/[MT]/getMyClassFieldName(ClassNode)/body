{
  String res=null;
  for (Iterator it=classNode.getFields().iterator(); it.hasNext(); ) {
    FieldNode f=(FieldNode)it.next();
    if (f.isStatic() && f.isSynthetic() && f.getName().startsWith("$myClass"))     res=f.getName();
  }
  return res;
}
