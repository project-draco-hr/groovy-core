{
  version++;
  if (strongMetaClass instanceof ExpandoMetaClass)   modifiedExpandos.remove(this);
  strongMetaClass=answer;
  if (strongMetaClass instanceof ExpandoMetaClass)   modifiedExpandos.add(this);
  weakMetaClass=null;
  updateMetaClass();
}
