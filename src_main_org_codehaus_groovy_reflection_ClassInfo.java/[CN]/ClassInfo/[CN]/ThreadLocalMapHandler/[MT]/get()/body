{
  LocalMap recent=recentThreadMap;
  if (recent != null && recent.myThread == Thread.currentThread())   return recent;
 else {
    final LocalMap res=super.get();
    recentThreadMap=res;
    return res;
  }
}
