{
  if (data == null) {
    return null;
  }
  if (data instanceof ClassInfo) {
    ClassInfo classInfo=(ClassInfo)data;
    if (classInfo.getCls() == klazz)     return classInfo;
    return null;
  }
  ClassInfo d[]=(ClassInfo[])data;
  for (  ClassInfo classInfo : d) {
    if (classInfo.getCls() == klazz)     return classInfo;
  }
  return null;
}
