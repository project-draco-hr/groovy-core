{
  int line=expr.getLineNumber();
  int col=expr.getColumnNumber();
  compilationUnit.getErrorCollector().addErrorAndContinue(new SyntaxErrorMessage(new SyntaxException(msg + '\n',line,col),source));
}
