{
  final Class aClass=getClass();
  if (aClass == myMetaClass) {
    if (staticMetaClass == null)     staticMetaClass=InvokerHelper.getMetaClass(this);
    return staticMetaClass;
  }
 else   return InvokerHelper.getMetaClass(aClass);
}
