{
  for (int i=0; i < methods.size(); i++) {
    MethodNode someMeth=(MethodNode)methods.get(i);
    if (someMeth.getName().equals(method.getName()) && parametersEqual(someMeth.getParameters(),method.getParameters())) {
      return i;
    }
  }
  return -1;
}
