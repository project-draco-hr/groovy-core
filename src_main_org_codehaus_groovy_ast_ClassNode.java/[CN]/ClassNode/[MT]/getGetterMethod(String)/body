{
  for (Iterator iter=methods.iterator(); iter.hasNext(); ) {
    MethodNode method=(MethodNode)iter.next();
    if (getterName.equals(method.getName()) && !"void".equals(method.getReturnType()) && method.getParameters().length == 0) {
      return method;
    }
  }
  return null;
}
