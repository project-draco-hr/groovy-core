{
  CompileUnit compileUnit=getCompileUnit();
  if (compileUnit != null) {
    if (compileUnit.getClass(type) != null) {
      return type;
    }
    try {
      compileUnit.loadClass(type);
      return type;
    }
 catch (    Throwable e) {
    }
  }
  return null;
}
