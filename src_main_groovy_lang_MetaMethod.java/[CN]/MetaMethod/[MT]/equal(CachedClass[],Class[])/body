{
  if (a.length == b.length) {
    for (int i=0, size=a.length; i < size; i++) {
      if (!a[i].getCachedClass().equals(b[i])) {
        return false;
      }
    }
    return true;
  }
  return false;
}
