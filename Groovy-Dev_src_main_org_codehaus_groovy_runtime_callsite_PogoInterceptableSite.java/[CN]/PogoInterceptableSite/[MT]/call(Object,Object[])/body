{
  if (receiver instanceof GroovyInterceptable)   return ((GroovyObject)receiver).invokeMethod(name,InvokerHelper.asUnwrappedArray(args));
 else   return CallSiteArray.defaultCall(this,receiver,args);
}
