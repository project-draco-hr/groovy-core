{
  final Expression fieldExpr=var(fNode);
  final Expression otherExpr=prop(other,fNode.getName());
  return or(and(identicalX(fieldExpr,var("this")),differentX(otherExpr,other)),and(differentX(fieldExpr,var("this")),identicalX(otherExpr,other)));
}
