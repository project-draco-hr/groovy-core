{
  super(mc);
  if (obj != null) {
    if (InvokerHelper.getMetaClass(obj.getClass()) == mc)     object=obj;
 else     object=NONE;
  }
  if (myMetaClass == null)   myMetaClass=InvokerHelper.getMetaClass(getClass());
}
