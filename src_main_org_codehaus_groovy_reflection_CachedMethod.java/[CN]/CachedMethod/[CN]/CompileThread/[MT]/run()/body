{
  try {
    while (true) {
      final CachedMethod method=(CachedMethod)queue.take();
      if (method != null) {
        method.compileMethod();
      }
    }
  }
 catch (  InterruptedException e) {
  }
}
