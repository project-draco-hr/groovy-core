{
  sw.write("\nSECTION);\n");
  int c;
  while ((c=br.read()) != -1) {
    if (c == '%') {
      c=br.read();
      if (c != '>') {
        sw.write('%');
      }
 else {
        break;
      }
    }
    sw.write(c);
  }
  sw.write("\nresult.write(<<<SECTION\n");
}
