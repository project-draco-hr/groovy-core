{
  variableStack.remove(v.getName());
  if (CREATE_DEBUG_INFO) {
    Label endl=new Label();
    cv.visitLabel(endl);
    cv.visitLocalVariable(v.getName(),BytecodeHelper.getTypeDescription(v.getTypeName()),v.getStartLabel(),endl,v.getIndex());
  }
}
