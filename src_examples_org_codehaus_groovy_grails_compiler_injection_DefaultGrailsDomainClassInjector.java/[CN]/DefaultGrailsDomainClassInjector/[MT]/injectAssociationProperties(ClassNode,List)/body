{
  for (Iterator i=propertiesToAdd.iterator(); i.hasNext(); ) {
    PropertyNode pn=(PropertyNode)i.next();
    if (!hasProperty(classNode,pn.getName())) {
      classNode.addProperty(pn);
    }
  }
}
