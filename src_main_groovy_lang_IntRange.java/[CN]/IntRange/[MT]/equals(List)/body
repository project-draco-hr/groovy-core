{
  int size=size();
  if (that.size() == size) {
    for (int i=0; i < size; i++) {
      if (!InvokerHelper.compareEqual(get(i),that.get(i))) {
        return false;
      }
    }
    return true;
  }
  return false;
}
