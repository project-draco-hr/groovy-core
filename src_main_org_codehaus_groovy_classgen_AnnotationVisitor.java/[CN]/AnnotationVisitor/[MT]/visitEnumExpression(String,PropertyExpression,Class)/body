{
  if (!isEnum(propExpr.getObjectExpression().getType().getTypeClass())) {
    addError("Attribute '" + attrName + "' should have type Enum, but found "+ propExpr.getObjectExpression().getType().getTypeClass().getName(),propExpr);
  }
}
