{
  exhausted=!delegate.hasNext();
  if (!exhausted) {
    next=delegate.next();
    if (!DefaultTypeTransformation.castToBoolean(condition.call(next))) {
      exhausted=true;
      next=null;
    }
  }
}
