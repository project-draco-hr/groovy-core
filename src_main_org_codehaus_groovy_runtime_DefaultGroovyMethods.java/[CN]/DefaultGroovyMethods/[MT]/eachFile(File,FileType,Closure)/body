{
  checkDir(self);
  final File[] files=self.listFiles();
  if (files == null)   return;
  for (  File file : files) {
    if ((fileType != FileType.FilesOnly && file.isDirectory()) || (fileType != FileType.DirectoriesOnly && file.isFile())) {
      closure.call(file);
    }
  }
}
