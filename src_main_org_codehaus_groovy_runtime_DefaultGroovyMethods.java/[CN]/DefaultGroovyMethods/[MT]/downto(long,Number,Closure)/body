{
  long to1=to.longValue();
  if (self >= to1) {
    for (long i=self; i >= to1; i--) {
      closure.callSpecial(new Long(i));
    }
  }
 else   throw new GroovyRuntimeException("Infinite loop in " + self + ".downto("+ to+ ")");
}
