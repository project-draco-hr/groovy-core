{
  if (isBigDecimal(left) || isBigDecimal(right)) {
    return toBigDecimal(left).divide(toBigDecimal(right),BigDecimal.ROUND_HALF_EVEN);
  }
 else   return new Double(left.doubleValue() / right.doubleValue());
}
