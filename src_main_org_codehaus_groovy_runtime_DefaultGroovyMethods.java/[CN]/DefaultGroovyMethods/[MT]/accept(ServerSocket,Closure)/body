{
  final Socket socket=serverSocket.accept();
  new Thread(new Runnable(){
    public void run(){
      try {
        closure.callSpecial(socket);
      }
  finally {
        try {
          socket.close();
        }
 catch (        IOException e) {
        }
      }
    }
  }
).start();
  return socket;
}
