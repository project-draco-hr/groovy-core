{
  try {
    closure.call(reader);
    Reader temp=reader;
    reader=null;
    temp.close();
  }
  finally {
    if (reader != null) {
      try {
        reader.close();
      }
 catch (      Exception e) {
        LOG.warning("Caught exception closing Reader: " + e);
      }
    }
  }
}
