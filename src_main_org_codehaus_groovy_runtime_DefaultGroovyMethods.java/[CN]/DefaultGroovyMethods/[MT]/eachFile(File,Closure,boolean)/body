{
  checkDir(self);
  final File[] files=self.listFiles();
  if (files == null)   return;
  for (  File file : files) {
    if (!onlyDir || file.isDirectory()) {
      closure.call(file);
    }
  }
}
