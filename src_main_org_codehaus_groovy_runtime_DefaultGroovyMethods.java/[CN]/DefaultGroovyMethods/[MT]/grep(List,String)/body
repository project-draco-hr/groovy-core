{
  List answer=new ArrayList();
  for (int i=0; i < self.size(); i++) {
    Object value=self.get(i);
    String s=toString(value);
    if (s.matches(regexp)) {
      answer.add(value);
    }
  }
  return answer;
}
