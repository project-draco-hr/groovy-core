{
  int i=n.intValue();
  int size=self.size();
  i=normaliseIndex(i,size);
  if (i < size) {
    self.set(i,value);
  }
 else {
    while (size < i) {
      self.add(size++,null);
    }
    self.add(i,value);
  }
}
