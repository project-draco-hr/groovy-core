{
  List<T> ret=new ArrayList<T>();
  while (self.hasNext()) {
    T value=self.next();
    if (DefaultTypeTransformation.castToBoolean(closure.call(value))) {
      ret.add(value);
    }
 else {
      break;
    }
  }
  return ret;
}
