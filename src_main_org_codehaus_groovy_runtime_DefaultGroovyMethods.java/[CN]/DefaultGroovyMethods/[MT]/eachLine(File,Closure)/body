{
  BufferedReader reader=newReader(self);
  try {
    while (true) {
      String line=reader.readLine();
      if (line == null) {
        break;
      }
 else {
        closure.call(line);
      }
    }
    reader.close();
  }
 catch (  IOException e) {
    if (reader != null) {
      try {
        reader.close();
      }
 catch (      Exception e2) {
      }
      throw e;
    }
  }
}
