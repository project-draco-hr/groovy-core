{
  int c;
  try {
    char[] chars=new char[1];
    while ((c=reader.read()) != -1) {
      chars[0]=(char)c;
      writer.write((String)closure.callSpecial(new String(chars)));
    }
  }
 catch (  IOException e) {
  }
}
