{
  try {
    closure.call(stream);
    InputStream temp=stream;
    stream=null;
    temp.close();
  }
  finally {
    if (stream != null) {
      try {
        stream.close();
      }
 catch (      Exception e) {
        log.warning("Caught exception closing InputStream: " + e);
      }
    }
  }
}
