{
  final Set ansSet;
  if (self instanceof SortedSet) {
    ansSet=new TreeSet(self);
  }
 else {
    ansSet=new HashSet(self);
  }
  if (self.size() > 0) {
    ansSet.removeAll(operands);
  }
  return ansSet;
}
