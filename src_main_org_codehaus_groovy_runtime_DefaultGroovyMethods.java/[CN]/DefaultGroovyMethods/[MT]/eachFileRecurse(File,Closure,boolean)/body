{
  checkDir(self);
  final File[] files=self.listFiles();
  if (files == null)   return;
  for (  File file : files) {
    if (file.isDirectory()) {
      closure.call(file);
      eachFileRecurse(file,closure,onlyDir);
    }
 else     if (!onlyDir) {
      closure.call(file);
    }
  }
}
