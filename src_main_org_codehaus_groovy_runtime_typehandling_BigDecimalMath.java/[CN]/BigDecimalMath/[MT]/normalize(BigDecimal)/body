{
  if (number.signum() == 0) {
    return number.setScale(0);
  }
  try {
    while (true) {
      number=number.setScale(number.scale() - 1);
    }
  }
 catch (  ArithmeticException e) {
    return number;
  }
}
