{
  MockCharStream in=new MockCharStream("cheddar");
  assertEquals('c',in.consume());
  assertEquals('h',in.consume());
  assertEquals('e',in.consume());
  assertEquals('d',in.consume());
  assertEquals('d',in.consume());
  assertEquals('a',in.consume());
  assertEquals('r',in.consume());
  assertEquals((char)-1,in.consume());
}
