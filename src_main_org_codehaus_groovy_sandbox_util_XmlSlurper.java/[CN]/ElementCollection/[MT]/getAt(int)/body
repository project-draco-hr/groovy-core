{
  int i=index;
  if (i >= 0) {
    final Iterator iter=iterator();
    while (iter.hasNext()) {
      if (i-- == 0) {
        return iter.next();
      }
 else {
        iter.next();
      }
    }
  }
  throw new ArrayIndexOutOfBoundsException(index);
}
