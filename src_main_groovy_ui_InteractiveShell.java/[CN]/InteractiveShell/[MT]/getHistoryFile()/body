{
  File history=new File(getGroovyUserDir(),"groovysh.history");
  if (!history.exists()) {
    try {
      history.createNewFile();
    }
 catch (    IOException ioe) {
      ioe.printStackTrace();
    }
  }
  return history;
}
