{
  Collection answer;
  if (left instanceof SortedSet) {
    answer=new TreeSet();
  }
 else   if (left instanceof Set) {
    answer=new HashSet();
  }
 else {
    answer=new ArrayList(newCapacity);
  }
  answer.addAll(left);
  return answer;
}
