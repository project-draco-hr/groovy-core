{
  String sql="select * from " + table;
  if (where != null) {
    sql+=" where ";
    if (parent != null && parent.where != null) {
      sql+=parent.getWhereSql() + " and ";
    }
    sql+=getWhereSql();
  }
  return sql;
}
