{
  handleBoolean();
  handleNullWithoutBoolean();
  handleInstanceCase();
  handleCollections();
  handleSAM();
  castToTypeFallBack();
  if (!handle.type().equals(callSite.type()))   castAndSetGuards();
}
