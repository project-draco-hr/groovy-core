{
  if (!finished && source != null) {
    if (consumed + k >= data.length()) {
      refill();
    }
    if (consumed + k < data.length()) {
      return data.charAt(consumed + k);
    }
  }
  return CharStream.EOS;
}
