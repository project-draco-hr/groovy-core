{
  if (receiver instanceof Class)   return createCallStaticSite(name,(Class)receiver,args);
  if (!(receiver instanceof GroovyObject)) {
    return createPojoSite(name,receiver,args);
  }
  return createPogoSite(name,receiver,args);
}
