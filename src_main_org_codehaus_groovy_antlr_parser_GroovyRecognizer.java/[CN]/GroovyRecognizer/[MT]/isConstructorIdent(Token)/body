{
  if (currentClass == null)   return false;
  if (currentClass.getType() != IDENT)   return false;
  String cname=currentClass.getText();
  if (x == null || x.getType() != IDENT)   return false;
  return cname.equals(x.getText());
}
