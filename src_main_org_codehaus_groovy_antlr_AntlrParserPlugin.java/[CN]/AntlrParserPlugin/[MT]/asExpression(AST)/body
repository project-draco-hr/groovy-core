{
  AST leftNode=node.getFirstChild();
  Expression leftExpression=expression(leftNode);
  AST rightNode=leftNode.getNextSibling();
  Type type=resolvedName(rightNode);
  return CastExpression.asExpression(type,leftExpression);
}
