{
  AST node=labelNode.getFirstChild();
  String label=identifier(node);
  Statement statement=statement(node.getNextSibling());
  if (statement.getStatementLabel() == null)   statement.setStatementLabel(label);
  return statement;
}
