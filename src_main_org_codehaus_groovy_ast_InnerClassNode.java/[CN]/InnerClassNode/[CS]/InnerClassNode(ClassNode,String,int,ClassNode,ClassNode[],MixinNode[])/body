{
  super(name,modifiers,superClass,interfaces,mixins);
  this.outerClass=outerClass;
  if (outerClass.innerClasses == null)   outerClass.innerClasses=new LinkedList<InnerClassNode>();
  outerClass.innerClasses.add(this);
}
