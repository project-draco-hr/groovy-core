{
  int index=Math.max(description.indexOf("<" + tag.toLowerCase() + ">"),description.indexOf("<" + tag.toUpperCase() + ">"));
  if (index < 0)   return description;
  return description.substring(0,index);
}
