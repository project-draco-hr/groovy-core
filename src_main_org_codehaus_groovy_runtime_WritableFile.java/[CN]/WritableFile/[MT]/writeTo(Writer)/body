{
  final Reader reader=(this.encoding == null) ? DefaultGroovyMethods.newReader(this.delegate) : DefaultGroovyMethods.newReader(this.delegate,this.encoding);
  try {
    int c=reader.read();
    while (c != -1) {
      out.write(c);
      c=reader.read();
    }
  }
  finally {
    reader.close();
  }
  return out;
}
