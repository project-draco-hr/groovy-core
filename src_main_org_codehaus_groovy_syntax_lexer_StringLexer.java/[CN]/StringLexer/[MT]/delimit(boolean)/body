{
  super.delimit(delimit);
  if (delimit) {
    try {
      if (!finished && la(1) == CharStream.EOS) {
        finishUp();
        if (!allowGStrings) {
          emptyString=true;
        }
      }
    }
 catch (    Exception e) {
      finished=true;
    }
  }
}
