{
  if (o == null)   return 0;
  if (o instanceof Entry)   return ((Entry)o).isValid() ? 1 : 0;
  final Entry[] arr=(Entry[])o;
  int count=0;
  for (  Entry entry : arr) {
    if (entry.isValid())     count++;
  }
  return count;
}
