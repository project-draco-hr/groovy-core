{
  int count=0;
  for (int i=0; i < segments.length; i++) {
    for (int j=0; j < segments[i].table.length; j++) {
      for (Entry e=segments[i].table[j]; e != null; e=e.getNext())       count++;
    }
  }
  return count;
}
