{
  while (true) {
    int c=reader.read();
    if (c == -1)     break;
    if (c == '%') {
      c=reader.read();
      if (c == '>')       break;
    }
    templateExpressions.append((char)c);
  }
  templateExpressions.append("; ");
}
