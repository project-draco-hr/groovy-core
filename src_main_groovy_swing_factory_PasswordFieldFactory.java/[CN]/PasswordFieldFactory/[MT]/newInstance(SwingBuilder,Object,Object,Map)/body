{
  if (SwingBuilder.checkValueIsTypeNotString(value,name,JPasswordField.class)) {
    return value;
  }
  if (value instanceof String) {
    return new JPasswordField((String)value);
  }
 else {
    return new JPasswordField();
  }
}
