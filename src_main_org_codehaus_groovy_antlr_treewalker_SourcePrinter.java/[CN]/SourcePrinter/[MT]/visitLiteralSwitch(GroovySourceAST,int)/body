{
  if (visit == OPENING_VISIT) {
    print(t,visit,"switch (");
  }
  if (visit == SUBSEQUENT_VISIT) {
    tabLevel++;
    print(t,visit,") {");
  }
  if (visit == CLOSING_VISIT) {
    print(t,visit,"}");
    tabLevel--;
  }
}
