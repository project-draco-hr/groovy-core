{
  if (getParentNode().getType() == GroovyTokenTypes.TYPE || getParentNode().getType() == GroovyTokenTypes.TYPECAST) {
    print(t,visit,null,null,"[]");
  }
 else {
    print(t,visit,"[",null,"]");
  }
}
