{
  if (value instanceof Action) {
    return new JToggleButton((Action)value);
  }
 else   if (value instanceof Icon) {
    return new JToggleButton((Icon)value);
  }
 else   if (value instanceof String) {
    return new JToggleButton((String)value);
  }
 else   if (value != null) {
    throw new RuntimeException(name + " can only have a value argument of type javax.swing.Action, javax.swing.Icon, or a String.");
  }
 else {
    return new JToggleButton();
  }
}
