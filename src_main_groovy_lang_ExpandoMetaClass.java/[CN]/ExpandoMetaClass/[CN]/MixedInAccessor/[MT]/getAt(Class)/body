{
  for (  MixinInMetaClass mixin : mixinClasses)   if (mixin.getMixinClass().getTheClass() == key)   return mixin.getMixinInstance(object);
  throw new RuntimeException("Class " + key + " isn't mixed in "+ object.getClass());
}
