{
  try {
    Constructor c=compileMethodHandle(method,loader);
    if (c != null)     return (MethodHandle)c.newInstance();
  }
 catch (  Throwable e) {
  }
  return new ReflectiveMethodHandle(method);
}
