{
  long start=System.currentTimeMillis();
  try {
    Thread.sleep(millis);
  }
 catch (  InterruptedException e) {
    long slept=System.currentTimeMillis() - start;
    long rest=millis - slept;
    if (rest > 0)     sleepImpl(object,rest);
  }
}
