{
  lock();
  try {
    SoftReference<T> resRef=value;
    T res;
    if (resRef != null && (res=resRef.get()) != null)     return res;
    res=initValue();
    value=new MySoftRef<T>(res);
    return res;
  }
  finally {
    unlock();
  }
}
